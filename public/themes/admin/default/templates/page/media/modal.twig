<div class="modal fade modal-media_{{ type_modal }}" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
    <div class="modal-dialog" style="max-width: 95%;">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Media</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-sm-2 col-add-media">
                        <div class="card mb-3">
                            <div class="card-body">
                                <form method="POST" enctype="multipart/form-data" class="form-add-media">
                                    <label for="pictures" class="label-add-media"><i class="fas fa-plus" aria-hidden="true"></i></label>
                                    <input type="hidden" name="MAX_FILE_SIZE" value="1000000"/>
                                    <input type="file" id="pictures" name="pictures" accept="image/*" class="input-add-media"/>
                                    <button type="submit" class="btn btn-secondary btn-add-media">Enregistré</button>
                                </form>
                            </div>
                            <div class="card-footer">
                                Max file size : 5M
                            </div>
                        </div>
                    </div>
                    {% for data in get_all_media %}
                    <div class="col-sm-2">
                        <div class="card mb-3">
                            <img class="card-img-top" src="{{ (data.slug ~ data.name)|base_url }}" alt="Card image cap">
                            <div class="card-body">
                                <h4 class="card-title">{{ data.name }}</h4>
                                <p class="card-text" style="text-align: center">
                                    <span class="btn btn-secondary choice_img_{{ type_modal }}" data-mediaid="{{ data.id }}" data-mediaslug="{{ (data.slug ~ data.name) }}" style="margin-left: auto;margin-right: auto;">Choisir</span>
                                </p>
                            </div>
                            <div class="card-footer">
                                <small class="text-muted">{{ data.date }}</small>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>