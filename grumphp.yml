parameters:
  git_dir: .
  bin_dir: vendor/bin
  tasks:
    phpcsfixer2:
      allow_risky: true
      cache_file: ~
      config: .php_cs
      using_cache: true
      config_contains_finder: false
      verbose: true
      diff: false
      triggered_by: ['php']
    phplint: ~
